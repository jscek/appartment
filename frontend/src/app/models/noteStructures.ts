export interface NoteStructure {
    id: number;
    title: string;
    description: string;
    createdAt: string;
    updatedAt: string;
    user_name: string;
}

export interface EditnoteStructure {
    id: number;
    title: string;
    description: string;
}