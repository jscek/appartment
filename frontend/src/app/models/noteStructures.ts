export interface NoteStructure {
    id: number;
    title: string;
    description: string;
    created_at: string;
    user_name: string;
}

export interface EditnoteStructure {
    id: number;
    title: string;
    description: string;
}